apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticket-service
  template:
    metadata:
      labels:
        app: ticket-service
    spec:
      containers:
        - name: ticket-service
          image: ticket-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: ticketing-config
                  key: DB_HOST
            - name: RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: ticketing-config
                  key: RABBITMQ_HOST
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
spec:
  selector:
    app: ticket-service
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30001
